export default function App() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-background text-foreground">
      <p>
        Фикстура-консьюмер: сюда CI устанавливает компоненты Lantern через
        <code> npx shadcn add @lantern/*</code> и проверяет, что проект собирается.
      </p>
    </main>
  );
}
